# Lessons Learned

- Make sure to enable the DLP API before running the script.
- Use `gcloud alpha dlp jobs list` to confirm job creation.
- Ensure your service account has the `roles/dlp.user` permission.
- Use consistent indentation in Python to avoid syntax errors.
